---
import { Icon } from 'astro-icon/components';

export interface Service {
  icon: string;
  title: string;
  description: string;
  /** Slug para enlazar a la página de detalle del servicio (opcional) */
  href?: string;
}

export interface Props {
  id?: string;
  /** Tagline arriba del título (texto pequeño en cyan/azul). Vacío = sin tagline. */
  tagline?: string;
  title?: string;
  subtitle?: string;
  items: Service[];
  /** Texto del enlace inferior de cada card. Si vacío, no se muestra. */
  cardCta?: string;
}

const {
  id = 'servicios',
  title = 'Soluciones para crecer con tecnología',
  subtitle = 'Cada servicio está diseñado para entregar resultados concretos: más ventas, menos trabajo manual y un negocio listo para crecer.',
  items,
  cardCta = 'Conversemos sobre tu proyecto',
} = Astro.props;
---

<section id={id} class="relative py-20 md:py-28 scroll-mt-[72px]">
  <div class="mx-auto max-w-7xl px-4 sm:px-6">
    <div class="mx-auto max-w-3xl text-center mb-14 md:mb-20">
      <h2
        class="text-3xl md:text-4xl lg:text-5xl font-bold tracking-tight text-slate-900 dark:text-white"
        data-reveal
      >
        {title}
      </h2>
      <p class="mt-5 text-lg text-slate-600 dark:text-slate-300" data-reveal data-reveal-delay="100">
        {subtitle}
      </p>
    </div>

    <div class="grid gap-6 md:gap-7 md:grid-cols-2 lg:grid-cols-3">
      {
        items.map((service, idx) => (
          <article
            class="card-glass group flex flex-col"
            data-reveal
            data-reveal-delay={Math.min(((idx % 3) + 1) * 100, 300).toString()}
          >
            <div class="relative inline-flex h-12 w-12 items-center justify-center rounded-xl bg-slate-100 dark:bg-white/[0.04] text-blue-600 dark:text-cyan-300 ring-1 ring-slate-200 dark:ring-white/10 transition-all duration-300 group-hover:text-white group-hover:bg-gradient-brand group-hover:ring-blue-400/40">
              <Icon name={service.icon} class="h-6 w-6" />
            </div>

            <h3 class="mt-5 text-xl font-semibold text-slate-900 dark:text-white">{service.title}</h3>
            <p class="mt-3 text-[15px] leading-relaxed text-slate-600 dark:text-slate-300 flex-grow">{service.description}</p>

            {cardCta && (
              <a
                href={service.href || '/contacto'}
                class="mt-6 inline-flex items-center gap-1.5 text-sm font-semibold text-blue-600 dark:text-indigo-300 transition-colors hover:text-blue-800 dark:hover:text-white"
              >
                {service.href ? 'Ver más detalles' : cardCta}
                <Icon
                  name="tabler:arrow-right"
                  class="h-4 w-4 transition-transform duration-200 group-hover:translate-x-1"
                />
              </a>
            )}
          </article>
        ))
      }
    </div>
  </div>
</section>
